 <h2>User info</h2>
        <div>
          <p><%= image_tag user.get_profile_image %></p>

          <p>name    <%=user.name  %></p>
          <p>introduction     <%=user.introduction  %></p>
          <p><%= link_to 'User_edit',edit_user_path(user.id)  %></p>

        </div>

          <h1>New book</h1>

       <%= form_with model: @book1=Book.new do |f| %>

          <h4>Title</h4>
            <%= f.text_field :title %>
          <h4>Opinion</h4>
            <%= f.text_area :body %>
          <%= f.submit 'Create Book' %>
       <% end %>