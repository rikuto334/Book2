<h1>Books#index</h1>
<% @books.each do |book| %>
<div>
  <%= image_tag book.user.get_profile_image %>
  <%= link_to book_path(book.id) do %>
  <p><%= book.title %></p>
  <%end%>

  <p><%= book.body %></p>

</div>
<% end %>

